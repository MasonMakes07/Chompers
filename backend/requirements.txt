# Kept for backward compatibility. The manifests now live at the repo root:
#   requirements.txt      - runtime deps (also what Vercel installs)
#   requirements-dev.txt  - runtime + local server + tests
-r ../requirements-dev.txt
