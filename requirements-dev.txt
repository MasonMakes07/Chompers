# Local development + tests. Installs the runtime deps plus the local ASGI
# server and the test tooling. Use this for running the app or the suite locally:
#   python -m pip install -r requirements-dev.txt
-r requirements.txt
uvicorn[standard]==0.52.1
pytest==9.1.1
pytest-asyncio==1.4.0
